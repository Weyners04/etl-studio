# frontend

Éditeur de flux visuel (React + React Flow). Producteur d'IR.

```bash
npm install
npm run dev   # http://localhost:5173
```

Arborescence : `src/ir` (types + sérialisation IR) · `src/editor` (canvas React Flow) ·
`src/api` (client backend).
